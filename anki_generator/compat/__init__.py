"""Legacy export groups; new code should import the owning modules directly.

These groups have moved under anki_generator.compat. They do not install aliases
for the former top-level module names.
"""
